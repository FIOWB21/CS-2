#include <iostream>

using namespace std;

int main()
{
    int size;
    cin >> size;
    int* arr = new int[size]{};
    cout << endl;
    for (int i = 0; i < size; i++) {
        cout << "arr[" << i << "] = ";
        cin >> arr[i];
    }
}
